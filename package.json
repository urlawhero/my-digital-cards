{
  "name": "my-digital-cards",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "echo 'ROOT BUILD SKIPPED' && exit 0",
    "install": "npm install --workspaces"
  },
  "devDependencies": {
    "prettier": "latest"
  }
}
